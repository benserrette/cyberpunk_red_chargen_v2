/**
 * Placeholder role lifepath table.
 *
 * Stubbed out until we add the official Cyberpunk Red role lifepath options.
 */
import { LifepathTable } from "@/classes/Lifepath";

const starting_table = new LifepathTable({
    name: "Role lifepath (stub)",
    rows: [],
});

export default starting_table;
